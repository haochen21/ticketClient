import { Cart } from './Cart';

export class CartPage {
    first: boolean;
    last: boolean;
    numberOfElements: number;
    totalElements: number;
    totalPages: number;
    size: number;
    content: Array<Cart>;
}